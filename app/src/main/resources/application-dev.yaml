spring:
  datasource:
    url: jdbc:tc:postgresql://xprojectdb
    username: root
    password: root
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
